#!/bin/sh
# User job ROLJ6100 — order processing
# OpenVMS analog: $ SET DEFAULT ROLLS$DATA  /  $ RUN ROLS6100
set -e
cd "$(dirname "$0")/.."
ROLLS_ROOT="$(pwd)"
# shellcheck source=../local-runtime/logicals.sh
. ./local-runtime/logicals.sh
./bin/ROLS6100
